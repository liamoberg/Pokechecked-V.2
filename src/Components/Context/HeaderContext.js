import { createContext } from "react";

const HeaderContext = createContext({});

export default HeaderContext;

// const headerContext = () => {
//     const [daysToShow, setDaysToShow] = useContext(5);
// };

// export default headerContext;
